import { Medicinedistribute } from './medicinedistribute';

describe('Medicinedistribute', () => {
  it('should create an instance', () => {
    expect(new Medicinedistribute()).toBeTruthy();
  });
});
